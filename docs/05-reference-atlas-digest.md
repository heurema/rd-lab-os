# Reference Atlas Digest

The source report argues that the field is converging on a loop:

```text
clarify scope → decompose → search iteratively → collect evidence → synthesize → cite/trace provenance
```

Immediate references to study:

1. STORM / Co-STORM — perspective expansion and outline-first synthesis.
2. GPT Researcher — planner/executor/publisher pattern.
3. Open Deep Research — configurable model/search/MCP architecture.
4. MindSearch — question graph and planner/searcher split.
5. PaperQA2 — evidence-first scientific RAG and contradiction handling.
6. OpenScholar / ScholarQA — literature-scale citation-backed synthesis.
7. GraphRAG — persistent memory over prior research artifacts.
8. DeepResearch Bench / DeepScholar-Bench — evaluation.
9. Codex / Claude / Gemini skills — portable workflow packaging.
10. last30days-style tools — social and recency signal layer.

Important operating distinction:

```text
reference ≠ dependency
pattern ≠ product
workflow ≠ backend
```
