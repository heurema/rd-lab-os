# 12 Memory Update

## Promote now

Do not update global memory.

## Candidate project-local memory

After one more successful ledger run, consider promoting:

```text
For rd-lab-os idea work, use run-local idea_ledger.csv before adding scripts or
global templates. Provider critique should update statuses, not replace
evidence rows.
```

## Do not promote

- `idea_ledger.csv` as required global template.
- `idea_scorecard.csv` as required artifact.
- `debate_ideas.py` as accepted next implementation.

## Follow-up trigger

Promote only if a future run proves that another agent can continue idea work
from `idea_ledger.csv` without reading the originating chat.
