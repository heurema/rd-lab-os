# 12 Memory Update

## Promote now

Do not update global memory from this run yet.

## Candidate memory after follow-up validation

If the next idea-ledger experiment succeeds, promote this pattern:

```text
For rd-lab-os idea work, create a project-local .heurema/rdlab run, populate
the evidence ledger before synthesis, use provider_router.py doctor --probe,
and record provider debate limitations explicitly.
```

## Do not promote

- Provider-generated ideas as durable facts.
- Vector DB or semantic clustering as accepted direction.
- Automated provider debate as required architecture.

## Project-local memory candidates

Consider adding a curated backlog later under:

```text
.heurema/rdlab/ideas/
.heurema/rdlab/experiments/
```

Only do that after at least one follow-up experiment validates the artifact
shape.
