# 12 Memory Update

## Promote now

Do not update global memory.

## Candidate project-local memory

If X1c succeeds:

```text
idea_ledger.csv can carry idea handoff between agents only when status update
rules are explicit. Provider refusal to update without new evidence is a valid
critic outcome.
```

## Do not promote

- Current ledger schema as template-ready.
- Agy `validated` status as accepted.

## Follow-up trigger

Promote only after `idea_ledger_v2.csv` prevents unsupported status upgrades in
a repeated provider handoff test.
